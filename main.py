"""Agentic Data Analyst — top-level entry point.

Layers are added one at a time per BUILD_PLAN.md. This file is a
placeholder until Layer 3, where the first end-to-end pipeline is wired
(query_planner -> sql_generator -> presenter). Do not add pipeline logic
here until then.
"""
from __future__ import annotations


def main() -> None:
    print("Agentic Data Analyst — skeleton only. See BUILD_PLAN.md.")


if __name__ == "__main__":
    main()
